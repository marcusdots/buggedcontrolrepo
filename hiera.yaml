---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: all-there-is
    paths: 
      - "node/%{trusted.certname}.yaml"
      - "datacenter/%{trusted.extensions.pp_datacenter}"
      - common.yaml

