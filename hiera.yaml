---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: per node basis
    path: certname/%{trusted.certname}.yaml
  - name: kind of sane defaults
    path: common.yaml

